---
import Button from "./Button.astro";
---

<div class="baggrund">
  <video class="bg-video" autoplay muted loop playsinline preload="auto">
    <source src="/image/infoside_video.mp4" type="video/mp4" />
  </video>

  <div class="content">
    <h1>
      Tilmeld dig intromøde og kom et skridt tættere på et unikt fælleskab.
    </h1>
    <Button title="Revervér en GRATIS plads" />
    <p>Kan altid fortrydes, så gør det før rejsen er væk</p>
  </div>
</div>

<style>
  .baggrund {
    position: relative;
    height: 60vh;
    overflow: hidden;
  }

  .bg-video {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: 0;
  }

  /* overlay OVER video */
  .baggrund::after {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.35);
    z-index: 1;
  }

  /* content OVER overlay */
  .content {
    position: relative;
    z-index: 2;
    height: 100%;
    display: grid;
    justify-items: center;
    align-content: center; /* center samlet */
    text-align: center;
    padding-top: 60px;
  }

  .content h1 {
    color: white;
    max-width: 700px;
    text-shadow: 0 5px 20px rgba(0, 0, 0, 0.938);
  }

  p {
    color: white;
    text-shadow: 0 2px 20px rgba(0, 0, 0, 0.6);
  }
</style>
